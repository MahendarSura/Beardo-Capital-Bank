
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  labels:
    app: beardo-Capital-Bank-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: beardo-Capital-Bank-backend
  template:
    metadata:
      labels:
        app: beardo-Capital-Bank-backend
    spec:
      containers:
        - name: backend
          image: your-dockerhub-username/beardo-Capital-Bank-backend:latest
          ports:
            - containerPort: 5000
          env:
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: backend-secrets
                  key: mongo-uri
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"

